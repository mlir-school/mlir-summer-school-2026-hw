[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tiny"
version = "0.1.0"
description = "Python DSL for MLIR Tutorial dialects"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["tiny*"]
